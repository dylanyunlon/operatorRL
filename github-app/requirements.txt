# GitHub App dependencies
PyGithub>=2.1.0
pydantic>=2.0.0
pyyaml>=6.0
cryptography>=42.0.0

# Agent OS (governance engine)
agent-os-kernel[full]

# Server (for local dev)
fastapi>=0.100.0
uvicorn>=0.20.0
